# よく使うコマンド チートシート（厳選20）

## 1. ターミナル基本（フォルダとファイルの操作）
- `pwd` : いま自分がいる場所（現在地）を表示する
- `ls` : フォルダの中身一覧を表示する（PowerShellで隠しファイルを見る時は `ls -Force`）
- `cd フォルダ名` : 指定したフォルダの中に入る
- `cd ..` : 1つ前のフォルダ（上の階層）に戻る
- `mkdir フォルダ名` : 新しいフォルダを作成する
- `echo "テキスト" > ファイル名` : ファイルを新規作成して文字を流し込む
- `cls` : ターミナルの文字を綺麗に掃除して画面をリセットする

## 2. Git基本サイクル（日常の記録・タイムマシン）
- `git init` : このフォルダをGitの管理下に置く（タイムマシンの起動）
- `git status` : 今の状態を確認する（赤＝作業中、緑＝セーブ準備完了）
- `git add ファイル名` : 特定のファイルを撮影台（ステージ）に乗せる
- `git add .` : 変更したすべてのファイルをまとめて撮影台に乗せる
- `git commit -m "メッセージ"` : シャッターを切ってアルバム（履歴）に保存する
- `git log` : 過去のセーブポイント（履歴）を一覧で見る
- `git diff` : 前回セーブした状態からどこを書き換えたか差分を見る

## 3. GitHub連携（クラウドとの同期）
- `git remote -v` : 送り先として登録されているGitHubのURLを確認する
- `git push`（または `git push origin main`）: 手元のセーブデータをGitHubに打ち上げる
- `git pull` : GitHub側にある最新の変更を手元にダウンロードして同期する

## 4. プロのショートカット・お助け技
- `Tab` キー : フォルダ名やファイル名を途中で自動補完する
- `Ctrl + C` : 動いている処理を強制ストップする
- `q` キー : `git log` などの閲覧画面から元の入力待ちに戻る